#include<stdio.h>
void dup_del(int n, char str){
     if(str[]==str[-1])

    
}
int main()
{
    int n;
    scanf("%d",&n);
    char str[n];
    scanf("%s",&str);
    dup_del(str,n);
    printf("%s",str[3]);
}